import ThemeProvider, { ThemeContext } from "@/app/theme-provider";

export default function CathalogLayout({ children }) {
    return (      
        <main>    
            {children}
        </main>
    );
  }